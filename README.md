# Static RSS Aggregator

This is a RSS aggregator that accumulates articles in static files.
It works actually both with RSS and with Atom feeds. New articles
are accumulated in files called `feed.xml` using original format.

